"""
Patches for webchat2api base.py — add GLM, Qwen, DeepSeek provider constants.

Apply these changes to: services/providers/base.py
"""

# ADD after existing provider constants:

GLM_PROVIDER = "glm"
QWEN_PROVIDER = "qwen"
DEEPSEEK_PROVIDER = "deepseek"

# UPDATE SUPPORTED_PROVIDERS:
SUPPORTED_PROVIDERS = {GPT_PROVIDER, GROK_PROVIDER, GEMINI_PROVIDER, GLM_PROVIDER, QWEN_PROVIDER, DEEPSEEK_PROVIDER}

# ModelCapability stays the same — "chat", "image", "image_edit", "video"
